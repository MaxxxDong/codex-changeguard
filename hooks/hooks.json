{
  "description": "Optional trusted SessionStart version-fingerprint check. Uses PLUGIN_ROOT and writable PLUGIN_DATA, stores state only under PLUGIN_DATA, emits no stdout when unchanged, and keeps manual scan/scan-system fallbacks available.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"$PLUGIN_ROOT/dist/hooks/session-start-entry.js\"",
            "commandWindows": "node \"%PLUGIN_ROOT%\\dist\\hooks\\session-start-entry.js\"",
            "timeout": 10,
            "statusMessage": "ChangeGuard version fingerprint check"
          }
        ]
      }
    ]
  }
}
